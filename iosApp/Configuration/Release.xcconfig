// Build parameters forwarded to Gradle by the "Compile Kotlin Framework" build phase.
// Accepted values and defaults live in build-logic ConventionBasePluginExtension.

SERVER_ENV = prod
IS_DEBUG = false
ENABLE_LOGS = false
